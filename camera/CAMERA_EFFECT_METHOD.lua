local EFFECT = require '/camera/CAMERA_EFFECT'

local SCRIPT = {
	[EFFECT.GENERIC] = {
		id = EFFECT.GENERIC,
		totalTime = 1.0,
		currentTime = 0,
		
		method = function(cameraSystem, self)
			
		end
	}
	--...
}

return EFFECT